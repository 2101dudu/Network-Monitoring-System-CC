Perguntas ao professor:
	- como delegar tarefas aos router? Podemos ter um json com formato 
    de dicionáro? Ou fazemos pré-processamento do json para o formato desejado?
    Ou fazemos processamento em run time?
    R: podemos fazer pré-processamento do json para o formato desejado.

    - o ID de um router irá vir do output do comando "whoami", no CORE?
    R: sim



A fazer:
	- separar Tasks de parser
    - trocar o que seja de IPs para DNS
    - relembrar que os packetIDs não estão a incrementar
    - separa funções do handletask.go



A referir no relatório:
	- uso cuidadoso de variáveis com espaço de memória reduzido
    - apresentar estrutura de um pacote como []byte, referindo, por exemplo: [pakcetType, Parameter1, Parameter2, Parameter3, ...]


Perguntas ao professor:
	- como delegar tarefas aos router? Podemos ter um json com formato 
    de dicionáro? Ou fazemos pré-processamento do json para o formato desejado?
    Ou fazemos processamento em run time?
    R: podemos fazer pré-processamento do json para o formato desejado.

    - o ID de um router irá vir do output do comando "whoami", no CORE?
    R: sim



A fazer:
    - trocar o que seja de IPs para DNS
    - VERIFICAR SE BYTE É SUFICIENTE PARA AGUENTAR COM TUDO — EM TODO O LADO
    
    
    - relembrar que os packetIDs não estão a incrementar
    - checksum



A referir no relatório:
	- uso cuidadoso de variáveis com espaço de memória reduzido
    - apresentar estrutura de um pacote como []byte, referindo, por exemplo: [pakcetType, Parameter1, Parameter2, Parameter3, ...]






SETUP DO DNS:
    clicar com o botao direto no router -> services -> userDefined -> "criar um novo ficheiro" -> adicionar texto no script:
        <nome>.sh
        echo "domain cc2024" >> /etc/resolv.conf
        echo "nameserver 10.0.x.x" >> /etc/resolv.conf

    no mesmo sítio -> StartupShutDown -> escrever:
        mkdir -p /run/systemd/resolve
        touch /run/systemd/resolve/resolv.conf
        sh <nome>.sh

    para correr o dns: service named start

    caso nao tiver /etc/bind -> instalar bind 9

    named.conf.local:
    zone "cc2024" IN {
        type master;
        file "etc/bind/zones/cc2024.zone";
        };


    >>cat db.127
    >>touch etc/bind/zones/cc2024.zone:
        mudar localhost para "cc2024"

    >>names-checkconf named.conf.local
    >>names-checkzone cc2024 zones/cc2024.zone
    >>sudo service named restart
    >>sudo service named status


    dentro do no NAmeserver:
    para correr o dns: service named start
        
 
